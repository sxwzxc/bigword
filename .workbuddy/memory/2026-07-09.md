# 2026-07-09

## 「动效字符」页面（/animate）最终实现：独立 Canvas 2D + requestAnimationFrame

- **定位**：完全独立实现，NOT import `bigword.ts` 任何东西（用户明确要求不要复用字符画页面的逻辑）。
- **核心引擎** `src/lib/animate-text.ts`：
  - 自带字体表 `ANIMATE_FONTS`（不共享 BUILTIN_FONTS）。
  - `AnimatedBigText` 类：离屏 `<canvas>` 把目标文字画成位图 → `getImageData` 按网格采样 alpha 得到"暗格 mask"（每暗格放一个小文字）。
  - `requestAnimationFrame` 逐帧 `ctx.fillText` 渲染每个暗格小文字，并对每格施加变换/颜色。
  - **随机缩放（默认动画 "随机缩放"）**：在 `compute()` 里给每个格子生成「决定性随机种子」`{phase, spd}`（由 col/row 异或哈希），`drawFrame` 中 `scale = lo + (hi-lo)*(0.5+0.5*sin(t*ω*spd+phase))`，每格以自己的相位+频率独立放大缩小、随时间持续变化。
  - **静态暗底**：先以低透明度（0.14）画一层静止的小字，保证"大形状(B)"始终清晰可读；亮色小字在其上各自跳动，读作"小A住在大B里"。
  - 其他动画：wave（列向波浪起伏）、scroll（素材字在格内纵向流动）、blink（逐格呼吸透明度）、rainbow（逐格 hsl 色相流动）、none。
  - 强度滑块控制 `scale`（hi），`lo = max(0.3, 2-hi)`；速度滑块控制周期；支持 `prefers-reduced-motion`（定格）。
  - `toPNG()` 导出当前帧为图片。
- **页面** `src/app/animate/page.tsx`：canvas 预览 + 控件（素材/目标文本、目标字体、素材字体、密度=列数、动画类型、速度、强度、文字色、背景色、下载图片）。默认演示 source="A"、target="B"、字体 Arial，直接对应"用A组成B、A随机缩放"的需求。
- **清理**：删除旧版 `globals.css` 的 `.asc-*` 整块与 `bigword.ts` 的 `buildAnimatedCells`/`AnimCell`/`AnimGrid`（仅旧 span 方案使用，已无人引用）。
- **验证**：`next build` 通过（/animate ~6.4 kB，无 lint 错误）；`next start` 冒烟测试 / 与 /animate 均 200，含 `<canvas>`。已提交并推送 main（f6c233c → 72f012e → 33f8266）。

## 后续迭代（同日）

- **纵向滚动改为像素级**（用户要求）：旧 `scroll` 是离散换字 `list[floor(...)]`，新实现为每格 `clip` 裁剪 + 连续亚像素 `off = (t*total/duration)%total` 偏移的垂直字流（`lineH=baseFont*0.95`，按 m 行循环 `list[(m%n+n)%n]`），平滑滚动而非跳字。
- **新增「随机飘动」效果 `drift`**（用户要求：A在B内随机飘动+大小变化，像随机撞击）：每格 `dx/dy` 用双正弦叠加（频率 `fx/fy`、相位 `px/py`）做伪随机游动，同步 `scale = lo+(hi-lo)*(0.5+0.5*sin(t*ω*fs+ps))`；`clip` 到单元格边界 → 看起来像在格内/在B里撞击。
- **扩展每格随机种子 `CellSeed`**：`{phase, spd, px, py, fx, fy, ps, fs}`（由坐标哈希 + `sin` 伪随机派生，确定性可复现），供 pulse/blink/drift 各自独立随机。
- **验证**：`next build` 通过（/animate 6.78 kB）；用 Playwright(完整chromium+系统字体) 真机验证：rAF 循环运行(40/1.2s)、canvas 656×716 真实内容、**随机缩放/纵向滚动(像素)/随机飘动 三模式前后帧均 differ**、无报错。已提交推送 `29ccc4e`。
- 注：`prefers-reduced-motion` 冻结已在上一轮移除（b50f2de），动效页面不再尊重该设置，始终运行 rAF。
